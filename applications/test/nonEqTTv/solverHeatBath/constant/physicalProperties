/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  13
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    format      ascii;
    class       dictionary;
    location    "constant";
    object      physicalProperties;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

// "type highEnthalpyThermo" carica la termodinamica del progetto (il bridge)
thermoType
{
    type            highEnthalpyThermo;
    mixture         coefficientWilkeMulticomponentMixture;
    transport       sutherland;
    // rrho e' il thermo del template del corso (copia di janaf); il thermo base
    // non entra nella fisica: T e Tve le ricava Mutation++ nel bridge
    thermo          rrho;
    energy          sensibleInternalEnergy;
    equationOfState perfectGas;
    specie          specie;
}

// impostazioni di Mutation++, uguali ai programmi 0D (la cartella dati la
// sceglie Allrun con MPP_DATA_DIRECTORY); il meccanismo dipende dalla figura
#include "$FOAM_CASE/conditions"

highEnthalpyMutation
{
    mixture                 air_5;
    stateModel              ChemNonEqTTv;
    thermodynamicDatabase   RRHO;
    mechanism               $mechanism;
    // esponente a della temperatura di Park T^a Tv^(1-a) per la dissociazione
    parkExponent            $parkExponent;
    // modelli del paper (sono anche i default): tau V-T delle eq. 9-17 come
    // in hy2Foam (mutation per quello di Mutation++) e Q_C-V preferenziale con
    // alpha = 0.3 (eq. 32, nonPreferential per l'eq. 31), come nel tutorial
    // heatBath di hy2Foam, che e' il caso della fig. 7
    relaxationTime          paper;
    chemistryVibration      preferential;
    preferentialFactor      0.3;
}

defaultSpecie N2;

#include "speciesThermo.janaf"

// ************************************************************************* //
